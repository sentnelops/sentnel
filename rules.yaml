rules:
  - id: block_delete
    match:
      tool: Bash
      pattern: "rm -rf"
    action: deny

  - id: block_sql_drop
    match:
      tool: Bash
      pattern: "drop table"
    action: deny

  - id: block_sensitive_files
    match:
      tool: Read
      path: [".env", "id_rsa", ".pem"]
    action: deny

  - id: network_egress
    match:
      tool: Bash
      pattern: "curl"
    action: deny
